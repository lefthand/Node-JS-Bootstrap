h1= title
h3 Here's the users
p There're right around here somewhere 
p 
  a(href='/user/create') Add User
p Oh right, here they are:
ul.users
  - each user in users
    li
      a(href='/user/#{user._id}') #{user.name} \n
- if (loggedInUser.is_admin) 
  ul.users\n
    - each user in users
      li\n
        a(href='/user/#{user._id}/remove') Remove #{user.name}
        |  (#{user._id})
        img(src='http://static1.robohash.com/#{user.name}')
//p Or another way
//ul.users
//  != partial('user', users);
//-p Or a third way
//-ul.users
//-  != partial('userGlobal', {collection: users, as: global});
//-p Or a forth way
//-ul.users
//-  != partial('userPerson', {collection: users, as: 'person'});

